# Phase 16b: Results Table -- Not Produced

This phase stopped at step 1 (edge filtering) because the tail-tier-restricted
training edge set (117 train edges, 37 unique target items) fell far short of
the meaningful-training threshold. Steps 2-4 (training and evaluation) were
never reached, so there is no trained checkpoint and no results to report
here.

See `edge_filtering_summary.md` for the step 1 count and check, and
`phase16b_notes.md` for the full verdict and reasoning. Phase 16's own
`../phase16_relevance_tail_dial/results_table.md` remains the standing
result for the relevance/tail-exposure dial thread.
