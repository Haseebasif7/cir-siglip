# Phase 31, Step 5: Ensemble Size Sweep -- NOT COMPLETED (budget constraint)

Not run, for the same reason as `individual_seeds.md`: no seeds beyond the single step-3 winner
(`models/ot31_budget_check_full.pt`) were trained, so there is no ensemble to sweep sizes over. See
`individual_seeds.md` and `phase31_notes.md` for the full disclosure.
